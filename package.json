{
  "name": "graphics-gallery",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd react-app && npm run dev",
    "build": "cd react-app && npm run build",
    "preview": "cd react-app && npm run preview"
  }
}
